export const setMonthIndex = (state, index) => {
    return (
        index || null //or null very important
    )
  };


  export const setsmallCalendarMonth = (state, index) => {
    return (
        index || null //or null very important
    )
  };


  export const setDaySelected = (state, index) => {
    return (
        index || null //or null very important
    )
  };

  export const setShowEventModal = (state, bool = false) => {
    return (
        bool || null //or null very important
    )
  };